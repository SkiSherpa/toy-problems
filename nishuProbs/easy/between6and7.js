// what should go where ? is to make the result always be between 6 and 7.
function update(arg) { return Math.random() + y * arg; }
let y = 2;
let x = 3;
const result = update(x);
console.log(result)
const res = update(x);
console.log(res)
